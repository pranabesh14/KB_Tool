# ─── Web / UI ─────────────────────────────────────────────────────────────────

streamlit>=1.35.0

# ─── Environment ──────────────────────────────────────────────────────────────

python-dotenv>=1.0.0

# ─── Azure OpenAI / LangChain ─────────────────────────────────────────────────

openai>=1.30.0
langchain>=0.2.0
langchain-openai>=0.1.8          # AzureChatOpenAI, AzureOpenAIEmbeddings
langchain-community>=0.2.0       # HuggingFaceEmbeddings, FAISS wrapper
langchain-core>=0.2.0
langchain-classic>=0.0.1         # ConversationEntityMemory (moved out of langchain core in 0.2+)
langchain-text-splitters>=0.2.0  # RecursiveCharacterTextSplitter (moved out of langchain core)

# ─── Vector store ─────────────────────────────────────────────────────────────

faiss-cpu>=1.7.4                 # use faiss-gpu if you have a CUDA GPU

# ─── Embeddings / Reranking ────────────────────────────────────────────────────

sentence-transformers>=2.7.0     # CrossEncoder + SentenceTransformer
huggingface-hub>=0.23.0

# ─── Document parsing ─────────────────────────────────────────────────────────

PyPDF2>=3.0.1

# ─── Audio / Video transcription ─────────────────────────────────────────────

openai-whisper>=20231117         # local speech-to-text — runs on your machine,
# no API key needed. Weights download once to
# ~/.cache/whisper/ on first use (~74 MB for base)
moviepy>=1.0.3                   # audio extraction from local video files
# bundles its own ffmpeg via imageio-ffmpeg
imageio-ffmpeg>=0.4.9            # pinned to ensure bundled ffmpeg binary is present
yt-dlp>=2024.1.1                 # platform URL support (Dailymotion, Vimeo, etc.)
# SSL errors auto-retried with –no-check-certificate

# ─── Numerics / ML utilities ──────────────────────────────────────────────────

numpy>=1.26.0
scipy>=1.11.0                    # WAV file reading without ffmpeg (wavfile + resample_poly)
scikit-learn>=1.4.0
pandas>=2.1.4

# ─── Azure Cognitive Services (optional – for Azure Speech fallback) ───────────

# azure-cognitiveservices-speech>=1.35.0

# — Confluence + SharePoint integration —————————————–

msal>=1.26.0                     # Azure AD OAuth 2.0 (Microsoft Authentication Library)
requests>=2.31.0                 # HTTP client for REST API calls
beautifulsoup4>=4.12.0           # HTML parsing for Confluence page body
mammoth>=1.6.0                   # Word .doc/.docx text extraction